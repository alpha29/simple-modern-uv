import uuid
from datetime import UTC, datetime
from pathlib import Path

from fastapi import BackgroundTasks, FastAPI, File, HTTPException, UploadFile

from {{package_module}}.job import (
    create_job,
    delete_job,
    get_job,
    update_job,
)

from {{package_module}}.settings import settings

app = FastAPI(
    title="{{package_name}} API",
    description="{{package_name}}",
    version="1.0.0",
)

@app.get("/")
async def root():
    """Root endpoint."""
    return {"message": "{{package_name}} API", "version": "1.0.0"}


@app.get("/health")
async def health_check():
    """Health check endpoint."""
    return {"status": "healthy", "service": "chemx-tool"}
